# Firebase Configuration
# Get these values from Firebase Console > Project Settings > General
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.firebaseio.com

# Optional: Firebase Service Account JSON (for production)
# Generate from Firebase Console > Project Settings > Service Accounts
# Either set this as a stringified JSON object:
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"..."}
# Or use application default credentials (for local development with gcloud CLI)

# Server Configuration
PORT=3000

# Optional: Override weekend-only posting for testing
ALLOW_WEEKDAY_POSTING=false
